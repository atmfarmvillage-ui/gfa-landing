# Anciens liens slug-dans-le-path -> redirection 301 vers la forme query string
# (CF Pages clean URLs casse les rewrites internes vers .html ; on utilise des 301)
/boutique/:slug        /produit?slug=:slug         301
/checkout/:slug        /checkout?slug=:slug        301
/access/:token         /access?t=:token            301
